# Rhino 8 宿主验收（未执行）

记录 Rhino 完整版本、Windows 版本和运行时。

- [ ] 从发行 ZIP 全新安装；没有加载错误，只有一个中文菜单。
- [ ] 打开 Params > Geometry 的下拉列表，点、圆、扭曲盒等 35 个工具名称显示中文；开着列表切换语言也同步。
- [ ] 放置 Circle 参数，在图标模式检查框上方名称；使用其他名称标注插件时单独记录插件名称与结果。
- [ ] 绘制后保存 GHX，Circle 原始名称没有因为瞬时汉化而写入文件；用户自定义昵称保持不变。
- [ ] 文件/编辑/显示/求解菜单显示中文，快捷键、动态最近文件菜单不受影响。
- [ ] 搜索 Move 和移动；记录原生搜索能否检索中文及英文。
- [ ] 新建 Move、Circle、Series、Merge；文字模式布局不截断，图标模式保持正常。
- [ ] 修改组件昵称，切换中英、禁用、重新启用，自定义昵称仍保留。
- [ ] 添加/删除组件、复制/粘贴、撤销/重做、切换文档、关闭重开均正常。
- [ ] 标准组件的输入输出参数连线位置正确，计算结果和启用前一致。
- [ ] 保存 GH 与 GHX，卸载插件后打开；GUID、昵称、连线、数据、求解结果一致。
- [ ] 打开包含第三方和自定义绘制组件的文档，未支持的对象保留原文。
- [ ] 高清屏幕和长名称不截断；大型文档无明显绘制回退。
- [ ] 分别测试所声明支持的现代 .NET 和 .NET Framework 运行时。
- [ ] 关闭汉化后菜单与名称恢复，卸载并重启后无插件残留行为。

当前只有命令行核心测试、安装脚本隔离测试和本机 SDK 编译验证，不得把这些替代 GUI 验收。
